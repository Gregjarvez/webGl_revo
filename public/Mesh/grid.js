import Cube from './cube.js';function createCube(row, col, spacing) {  const cube = Cube(1,1,1);  cube.position.set(      row * spacing,      cube.geometry.parameters.height / 2,      col ? col * spacing : cube.position.z  )  return cube;}function BoxGrid(amount, spacing) {  const group = new THREE.Group();  for (let i = 0; i < amount; ++i) {    const cube = createCube(i, null, spacing);    group.add(cube);    for (let j = 1; j < amount; ++j) {      const cube = createCube(i, j, spacing);      group.add(cube);    }  }  group.position.x = -(spacing * (amount - 1)) / 2;  group.position.z = -(spacing * (amount - 1)) / 2;  return group;}export default BoxGrid;